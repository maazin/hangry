[pytest]
asyncio_mode = auto
# Fixtures share the loop of the test that uses them; only the one-time
# schema setup runs on the session loop (marked explicitly in conftest).
asyncio_default_fixture_loop_scope = function
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
